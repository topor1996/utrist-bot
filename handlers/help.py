"""
Обработчик команды /help
"""
from telegram import Update
from telegram.ext import ContextTypes
from keyboards.main_menu import main_menu_keyboard
from config import COMPANY_PHONE


async def help_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик команды /help"""

    help_text = """
📚 **Справка по боту**

**Основные команды:**
/start — Начать работу с ботом
/help — Показать эту справку
/menu — Вернуться в главное меню

**Что умеет бот:**

📋 **Наши услуги** — просмотр каталога юридических услуг с ценами

📞 **Записаться на консультацию** — оставить заявку на консультацию с юристом

❓ **Задать вопрос** — отправить вопрос юристу, ответ придёт в этот чат

ℹ️ **О компании** — информация о компании "Ваш юрист"

📍 **Контакты** — адрес, телефон, режим работы

**Как записаться:**
1. Нажмите "📞 Записаться на консультацию"
2. Выберите услугу
3. Введите ваше ФИО, телефон и email
4. Подтвердите заявку

После отправки заявки с вами свяжется менеджер для подтверждения времени.

**Есть вопросы?**
Позвоните нам: {phone}
"""

    await update.message.reply_text(
        help_text.format(phone=COMPANY_PHONE),
        parse_mode='Markdown',
        reply_markup=main_menu_keyboard()
    )


async def menu_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик команды /menu - алиас для главного меню"""
    from handlers.start import main_menu_handler
    await main_menu_handler(update, context)
