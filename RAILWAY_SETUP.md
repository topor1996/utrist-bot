# Пошаговая настройка на Railway.app

## Шаг 1: Подключение проекта к Railway

У вас есть два варианта:

### Вариант А: Через GitHub (рекомендуется)

1. Создайте репозиторий на GitHub:
   ```bash
   cd /Users/t1/Desktop/work/utrist-bot
   git init
   git add .
   git commit -m "Initial commit"
   ```
   
2. Создайте репозиторий на GitHub.com и выполните:
   ```bash
   git remote add origin https://github.com/ваш-username/utrist-bot.git
   git push -u origin main
   ```

3. В Railway: Settings → Connect GitHub Repo → выберите репозиторий

### Вариант Б: Прямая загрузка файлов

1. В Railway откройте ваш проект
2. Нажмите на сервис (если его нет, создайте "New Service")
3. Settings → Source → "Upload from local directory"
4. Выберите папку `/Users/t1/Desktop/work/utrist-bot`

## Шаг 2: Настройка переменных окружения

1. В Railway откройте ваш проект
2. Выберите сервис (или создайте новый)
3. Перейдите на вкладку **"Variables"**
4. Добавьте следующие переменные:

```
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id
DATABASE_URL=sqlite:///bot_database.db
WORK_START_HOUR=9
WORK_END_HOUR=18
WORK_DAYS=1,2,3,4,5
```

**Как получить BOT_TOKEN:**
- Найдите @BotFather в Telegram
- Отправьте `/newbot`
- Следуйте инструкциям
- Скопируйте токен

**Как получить ADMIN_IDS:**
- Найдите @userinfobot в Telegram
- Отправьте `/start`
- Скопируйте ваш ID (число)

## Шаг 3: Настройка команды запуска

1. В Railway откройте Settings вашего сервиса
2. Найдите раздел **"Deploy"**
3. В поле **"Start Command"** укажите:
   ```
   python bot.py
   ```

Railway автоматически:
- Определит Python из `runtime.txt`
- Установит зависимости из `requirements.txt`
- Запустит бота

## Шаг 4: Проверка работы

1. Перейдите на вкладку **"Deployments"**
2. Дождитесь успешного деплоя (зеленая галочка)
3. Откройте вкладку **"Logs"** - там будут логи работы бота
4. Найдите вашего бота в Telegram и отправьте `/start`

## Если что-то не работает:

### Бот не запускается:
- Проверьте логи в "View Logs"
- Убедитесь, что все переменные окружения добавлены
- Проверьте, что BOT_TOKEN правильный

### Ошибка импорта:
- Проверьте, что все файлы загружены
- Убедитесь, что `requirements.txt` содержит все зависимости

### База данных не создается:
- Проверьте права на запись (Railway должен создавать файлы автоматически)
- Убедитесь, что файл `database/init.sql` загружен

## Готово! 🎉

Ваш бот должен работать 24/7 на Railway!
